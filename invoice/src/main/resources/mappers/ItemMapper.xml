<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="az.cybernet.invoice.repository.ItemRepository">
    <insert id="addItems" parameterType="java.util.List">
        INSERT INTO items (
        invoice_id,
        name,
        unit_price,
        quantity,
        total_price,
        measurement_name,
        is_active,
        created_at,
        status
        )
        VALUES
        <foreach collection="items" item="item" separator=",">
            (
            #{item.invoice.id},
            #{item.name},
            #{item.unitPrice},
            #{item.quantity},
            #{item.totalPrice},
            #{item.measurement.name},
            #{item.isActive},
            #{item.createdAt},
            #{item.status}
            )
        </foreach>
    </insert>

    <!-- SELECT -->
    <select id="findById" parameterType="long" resultMap="ItemEntityResultMap">
        SELECT id,
               name,
               unit_price,
               quantity,
               total_price,
               is_active,
               created_at,
               updated_at,
               measurement_id,
               invoice_id
        FROM items
        WHERE id = #{id}
    </select>

    <select id="findAllItemsByInvoiceId" parameterType="long" resultMap="ItemEntityResultMap">
        SELECT id,
               name,
               unit_price,
               quantity,
               total_price,
               is_active,
               created_at,
               updated_at,
               measurement_name,
               invoice_id
        FROM items
        WHERE invoice_id = #{invoiceId}
          AND is_active = true
        ORDER BY id
    </select>

    <!-- UPDATE -->
    <update id="updateItems" parameterType="az.cybernet.invoice.entity.ItemEntity">
        UPDATE items
        SET name           = #{name},
            unit_price     = #{unitPrice},
            quantity       = #{quantity},
            total_price    = #{totalPrice},
            is_active      = #{isActive},
            updated_at     = #{updatedAt},
            measurement_id = #{measurement.id},
            invoice_id     = #{invoice.id}
        WHERE id = #{id}
    </update>

    <update id="deleteItemsByItemsId">
        UPDATE invoice.items
        SET is_active = false,
        updated_at = NOW()
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>
    <update id="deleteItemsByInvoiceId">
        UPDATE invoice.items
        SET is_active  = false,
            updated_at = NOW()
        WHERE invoice_id = #{invoiceId}
    </update>


    <resultMap id="ItemEntityResultMap" type="az.cybernet.invoice.entity.ItemEntity">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="unitPrice" column="unit_price"/>
        <result property="quantity" column="quantity"/>
        <result property="isActive" column="is_active"/>
        <result property="totalPrice" column="total_price"/>

        <association property="invoice" javaType="az.cybernet.invoice.entity.InvoiceEntity">
            <id property="id" column="invoice_id"/>
        </association>

        <association property="measurement" javaType="az.cybernet.invoice.entity.MeasurementEntity">
            <result property="name" column="measurement_name"/>
        </association>
    </resultMap>

</mapper>
